# Traffic Menace 像素美术方向 v0.1

> 本文件是《Traffic Menace》首轮垂直切片的整体美术方向。道路细节继续参考 `ROAD_ART_DIRECTION.md`；这里补充车辆、道具、事故、特效、HUD 和字体的统一规则。

## 1. 设计目标

玩家扮演一辆故意拖慢交通的小破车。画面要让玩家在一秒内读出三件事：

1. 哪辆车准备从哪一侧超车。
2. 哪个物件会改变路线、速度或可通行空间。
3. 哪次事故是自己制造的，而不是随机发生的。

首轮视觉基调为 **笨拙街机卡通**：厚实、略旧、反应夸张，但信息层级清楚。视觉趣味来自不协调的车辆、家具和施工物件撞在一起，而不是来自复杂背景或写实材质。

## 2. 技术画面

| 项目 | 固定值 |
| --- | --- |
| 引擎 | Godot 4.7，2D，Compatibility renderer |
| 逻辑画布 | 640×360 |
| 当前参考窗口 | 1280×720，整数 2 倍显示 |
| 摄像机 | 顶视角，车辆朝屏幕上方行驶 |
| 道路 | 五车道，当前灰盒坐标和碰撞尺寸保持不变 |
| 采样 | 最近邻；像素资源关闭平滑过滤和不必要的 mipmap |
| 移动 | 关键位置使用整像素；不依赖亚像素抖动、模糊或泛光 |
| 源文件 | 手工像素网格源稿；正式运行资源使用 PNG，道路 SVG 继续作为现有编辑源和灰盒回退 |
| 运行时比例 | 逻辑资产按 2 倍显示；不单独拉伸关键 Sprite |

### 2.1 锚点和尺寸

车辆统一使用底部中心锚点。实际可见车身不能超出当前模拟碰撞边界；画布额外保留 4 个逻辑像素边距，用来容纳阴影、灯光和事故变形。

当前灰盒的运行时车身尺寸折算到逻辑尺寸后为：

| 类型 | 逻辑碰撞参考 | 推荐逻辑画布 |
| --- | ---: | ---: |
| 玩家 / Sedan | 34×59 / 32×56 | 40×68 |
| SUV | 35×59 | 44×68 |
| Van | 38×66 | 48×76 |
| Pickup | 36×62 | 44×72 |
| Truck | 42×75 | 52×84 |
| Bus | 46×88 | 56×96 |

画布尺寸是美术资源的外框，不得被当作碰撞尺寸。模拟仍使用 `main.gd` 中的真实车身尺寸。

## 3. 视觉系统

### 3.1 形状和轮廓

- 车辆和障碍采用厚实块面，避免细长、写实和过多小结构。
- 车辆前后必须有不同的形状线索：前灯、挡风玻璃、尾灯和后备箱轮廓不能合并。
- Sedan 低矮、SUV 厚高、Van 近矩形、Pickup 有外露货斗、Truck 有长车头或货箱、Bus 有长车窗带。
- 玩家车使用橙色车身、深蓝玻璃和不对称小破损，成为画面中最容易定位的主体。
- 轮廓优先于内部细节；在 640×360 原生尺寸下，缩小查看仍要能分辨车辆类别。
- 轮廓主要使用 1–2 个逻辑像素的墨色边缘，局部允许用同色深色块代替完整描边。

### 3.2 明度、材质和光照

- 默认光照从左上方来，所有车辆、家具、道路和 UI 保持一致。
- 主体最多使用基础色、阴影色、高光色三层明度。
- 沥青用稀疏聚簇和少量补丁表达，不使用连续渐变和随机噪点。
- 金属用短高光块和冷色阴影表达；布料用大块褶皱；玻璃用单向深色反光；橡胶使用近墨色和一处灰色高光。
- 阴影只说明接地和重量，不能制造与碰撞边界冲突的假墙或假障碍。

### 3.3 动画性格

- 车辆正常运动克制，事故和道具动作夸张。
- 重要动作使用预制离散帧，不对关键车辆做任意角度实时旋转。
- 小事件用 2–4 帧，中型事件用 4–8 帧，大型事故用 8–16 帧。
- 车辆第一帧和最后一帧保持相同底部中心锚点，禁止脚下滑动式跳位。
- 事故动画可以压扁、拉长、弹起和回正，但不能修改模拟层位置或碰撞结果。

## 4. 固定调色板

颜色是角色，不是每张图独立选择的装饰。允许在同一角色内使用明度变化，但不新增近似颜色堆叠。

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 墨色轮廓 | `#091018` | 轮廓、深阴影、分隔线 |
| 沥青深色 | `#26323B` | 主道路底色 |
| 沥青亮面 | `#35464C` | 纹理聚簇、补丁和边缘 |
| 路肩深色 | `#17222D` | 人行道、路肩和背景 |
| 暖白 | `#F2E7C9` | 车道线、反光点、普通高亮 |
| 青蓝 | `#63C7D9` | 合流、有效窗口、信息提示 |
| 青绿 | `#58E1C1` | 可互动、成功、玩家正向反馈 |
| 橙黄 | `#FFCB5C` | 施工、等待、预警、选中 |
| 危险红 | `#FF6B6B` | 刹车、侧撞、耐久损失 |
| 奖励黄 | `#FFD75A` | Chaos、Combo、新纪录 |
| 信息蓝 | `#9AC7FF` | 司机意图、道路说明 |
| 玩家橙 | `#F08A5D` | 玩家车身主色 |

车辆可以在蓝、紫、黄、绿、橙、粉之间区分，但所有颜色都必须压在上述角色的明度和饱和度范围内；类型不能只靠颜色识别。

## 5. 资产家族

### 5.1 道路和场景

- 沥青逻辑瓦片：64×128，透明度关闭，可平铺，不能烘入车道线。
- 路肩 / 人行道逻辑瓦片：48×128，透明度关闭，可平铺，只说明边界。
- 车道线、反光道钉和动态道路滚动继续由运行时绘制。
- 合流路段使用动态几何，像素资源只提供导流箭头、边缘标记和等待/目标提示。
- 封道、抛锚和支路合流各提供一套远距离可读的事件标识。
- 场景细节密度低于车辆、道具和事故特效；HUD 安全区域不放高对比地标。

### 5.2 车辆

每类车辆提供一个中性基底图集，以及刹车灯、转向灯、意图和事故覆盖层：

- `cruise`：2 帧轻微车身/灯光变化。
- `brake`：2–4 帧尾灯和轮胎印反馈。
- `prepare_overtake` / `lane_change`：4 帧方向灯、压线和轻微前倾。
- `slip`：4–8 帧横向拉伸、轮胎印和回正。
- `roll`：8–12 帧离散翻滚角度。
- `crash` / `recover`：4–8 帧压扁、弹回和残留碎片。
- `despawn`：4 帧淡出或驶离，不使用柔和透明渐变。

### 5.3 八个切片道具

每个道具统一提供 24×24 图标和一个世界表现图集，图集按 `preview / commit / impact / cleanup` 阶段组织。

| 道具 | 视觉动词 | 关键画面 |
| --- | --- | --- |
| 漏油 | 滑 | 不规则油迹、短促冷色反光、车辆横向甩尾 |
| 掉沙发 | 挡 | 沙发落下弹跳、尘土、单车道绕行 |
| 滚轮胎 | 滚 | 轮胎跨车道摆动、碰撞后弹开 |
| 假转向灯 | 误导 | 错误方向箭头、闪烁、NPC 犹豫 |
| 道路施工牌 | 封 | 折叠牌展开、橙黄斜纹和封道标记 |
| 巨大床垫 | 弹 | 远处飞行预告、落地压扁、反弹 |
| 假刹车灯 | 误导 | 玩家尾灯异常亮起、后车急刹灯波 |
| 宽体改装 | 外扩 | 车身侧翼变宽、压线擦火花、代价提示 |

### 5.4 事故和特效

首轮实现三套可复用事故模板：

1. **橡皮筋侧滑**：车身先拉长，再横向滑出，尾部留下轮胎印。
2. **弹簧翻滚**：障碍命中后弹起，离散旋转，落地压扁后回弹。
3. **手风琴追尾**：前车压扁，后车依次叠上，连锁提示沿车辆队列弹出。

通用特效包括油迹反光、轮胎印、尘土、火花、碎片、像素烟雾、警示箭头、命中闪白和连锁编号背景。粒子只能发射这些离散贴片，不能使用软圆、泛光或运动模糊。

### 5.5 UI 和字体

- 面板采用 2 逻辑像素边框和 9-slice 思路，边角不可被拉伸变形。
- 道具槽提供空槽、普通、选中、冷却、禁用和被动状态。
- 事故记录采用“发生了什么 / 为什么发生”两行短句；文字、数字和按钮标题由代码绘制。
- 使用许可证清晰的中文像素字体，固定正文 12px、HUD 数值 16px、重要提示 20–24px、标题 32px。
- 目标板可以有说明文字，但正式游戏图片不得烘入动态文本。

## 6. 文件和命名规范

```text
prototypes/traffic-menace-graybox/assets/
  road/
  vehicles/
  items/
  vfx/
  ui/
  style_target/
```

- 文件名使用小写蛇形：`vehicle_npc_sedan_atlas.png`、`item_oil_leak_atlas.png`。
- 稳定 ID 使用 `vehicle_*`、`item_*`、`vfx_*`、`ui_*`、`road_*`。
- 图集帧顺序固定，帧编号从 `00` 开始；同一图集的所有帧使用同一画布和锚点。
- 透明 Sprite 必须保留完整画布，禁止自动裁切导致锚点漂移。
- 逻辑源图按 1 倍保存；需要 2 倍预览时只能用最近邻派生。
- 每个资源都要在 `asset-manifest.json` 记录来源、许可证、尺寸、帧数、锚点和验收状态。
- manifest 中的 `source_ref` 和 `verification_ref` 统一解析到 `family_defaults`；有实际来源的资源必须覆盖默认值。

## 7. 目标板验收

目标板必须同时展示普通和拥挤两种阅读条件，并满足：

- 640×360 原生尺寸下能分辨玩家车、至少两类 NPC、漏油、沙发、事故和 HUD。
- 车辆意图至少有方向灯、颜色/形状和文字以外的视觉线索。
- 事故结果在车辆密集时仍能通过形状、闪白、跳字位置或颜色定位。
- 1280×720 整数放大后没有模糊、透明边缘、纹理泄漏或非整数抖动。
- 道路是低对比背景，车辆、道具和事故是明确焦点。
- 目标板未通过前，不批量扩展 23 个道具或新增道路主题。

## 8. Godot 接入边界

首轮视觉接入只替换表现层，不改变交通模拟、碰撞、车道、事件账本和烟测：

- `vehicle_visuals[kind][state]` 映射车辆基底和状态覆盖层。
- `item_visuals[item_key][phase]` 映射图标、预告、世界表现和清理帧。
- `feedback_preset[small|medium|large]` 映射帧数、持续时间、闪白和镜头反馈。
- 缺失或未批准的资源回退到当前程序化绘制。
- 表现变形作用于视觉代理，不移动碰撞体。
- 如增加 GDScript 私有方法，使用中文 JavaDoc 风格注释。

## 9. 范围与状态

首轮正式设计包括目标板、道路、6 类车辆、8 个切片道具、3 类事故、通用特效、HUD 和字体方向。其余 15 个道具只在清单中保留 `backlog` 记录，不生成正式图像。

当前道路 SVG 和 Arial Unicode MS 仍是灰盒回退资源；只有通过原生尺寸、Godot 窗口和拥挤场景验收后，PNG 或正式字体才能标记为 `approved`。
