# Traffic Menace 道路美术与汇入方案

## 目标

当前原型采用 1280×720、顶视角、五道主路、程序化 Node2D 绘制。道路需要先做到“能读、能重复、能支持玩法”，再逐步替换成正式像素素材；因此本阶段不引入 3D 建模、UV 或复杂场景资产。

视觉目标是统一的像素风城市快速路：深色沥青作为低对比背景，暖白车道线负责导航，青绿色用于可互动的合流提示，橙黄色用于施工、等待和危险。车身、道具、特效、文字和 HUD 都必须遵循同一套像素网格、有限调色板和硬边形状语言，避免出现“像素车辆 + 平滑矢量特效 + 普通字体”的混合感。

## 像素风硬规则

- 正式像素画布目标为 640×360 逻辑像素，1280×720 使用整数 2 倍放大作为参考窗口。
- 道路材质、导流线、路肩、磨损和事件标识在逻辑像素尺寸上验收；运行时使用最近邻采样，不依赖抗锯齿。
- 纹理使用有限色板和成组像素块表现沥青颗粒、补丁、路缘和阴影；不使用平滑渐变、泛光、随机噪点或照片式纹理。
- 车道线、箭头、导流区和危险提示使用整数字号和阶梯式边缘；斜线不能出现模糊的半像素边。
- 施工牌、合流标识和道路事件文字使用中文像素字体或像素化字形；当前灰盒字体仅用于临时显示。
- SVG 继续作为可编辑的源稿格式，但不代表正式运行时的视觉结果。正式资源必须栅格化到固定像素网格，并在原生尺寸和拥挤车流场景中检查。
- 道路表现不能抢过车辆意图、事故结果和玩家可操作区域；背景细节密度必须低于交互元素。

### 建议色彩角色

| 角色 | 起始方向 | 用途 |
| --- | --- | --- |
| 沥青深色 | 深蓝灰 | 大面积背景与低对比底色 |
| 沥青亮面 | 蓝灰 | 纹理聚簇、车道边缘和磨损 |
| 暖白 | 米白 | 车道线、普通文字和高亮轮廓 |
| 青绿 | 青绿色 | 可互动合流、有效窗口和正向反馈 |
| 橙黄 | 橙黄色 | 施工、等待、预警和高风险事件 |
| 危险红 | 红珊瑚色 | 侧撞、耐久损失和事故警示 |
| 奖励黄 | 金黄色 | Chaos、Combo、奖励和新纪录 |
| 墨色 | 深靛黑 | 轮廓、阴影和 UI 分隔 |

具体色值可以调整，但颜色角色不能在不同资产中随意改变。

## 技术框架

| 项目 | 当前值 |
| --- | --- |
| 参考画布 | 1280×720（正式逻辑画布目标 640×360，整数 2 倍显示） |
| 主路 | 5 车道，510px 总宽，102px 单道 |
| 摄像机 | 顶视角、玩家固定在屏幕约 25% 高度 |
| 材质 | 当前为 128×256 SVG 源稿 + 运行时 ImageTexture；正式目标为 64×128 逻辑栅格或对应整数倍图集；不烘焙车道线 |
| 采样 | 最近邻、硬边、整像素对齐；车道线和动态磨损由运行时绘制 |
| 合流入口 | 由合流车辆触发；道路段从屏幕上方 `y=-260` 进入，单侧最多 2 段且间距至少 260px，抵达约 62% 高度的判定线后并入 |

## 道路材质层

1. **Sidewalk / Shoulder**：混凝土板、低对比接缝、深色路缘；只说明道路边界，不承担碰撞。
2. **Asphalt**：深蓝灰沥青、稀疏像素颗粒和补丁；纹理不包含车道线，便于改变车道数量和道路事件。
3. **Lane Marking**：运行时绘制暖白虚线和反光道钉，统一随正向 `road_scroll` 向下滚动；封道、压线和变道时仍保持清楚。
4. **Merge Marking**：左右支路采用相同沥青但加宽路肩、三角导流线、箭头和“汇入 L1/L5”提示；这些标线只存在于对应的动态道路段，不存在合流车辆时不绘制。

### 道路像素验收

- 在 640×360 逻辑尺寸下，车道边界、导流线和事件标记必须清楚，不依赖放大查看。
- 在 1280×720 参考窗口下不能出现双线、模糊边缘、纹理泄漏或非整数抖动。
- 沥青纹理重复 3×3 时不能出现明显的接缝、周期性亮斑或“贴图墙”。
- 车道线和道路事件的像素密度必须低于车辆和道具的细节密度。
- 任何道路装饰都不能产生与碰撞边界相冲突的假阴影或假障碍。
- 施工、合流和危险文字必须使用正式像素字体，并在中文较长文本下保持容器不溢出。

## 真正的多车道汇入

支路车辆使用两段式路线；第一段同时驱动一段临时道路几何：

```text
支路排队 → 动态道路段从屏幕上方进入 → 判定线检查真实空隙 → 最近外侧主路车道 → 普通 AI 逐条相邻变道
```

- 左支路的第一落点固定为 L1，右支路固定为 L5。
- 合流车辆生成时创建同 ID 的道路段，记录 `side`、`target_lane`、`y`、`phase` 和 `moving`；起点在屏幕上方，随镜头向下移动。
- 道路段尚未抵达判定线时，车辆固定在 `junction.y + MERGE_ENTRY_Y_OFFSET`，不提前进入主路；入口没有真实前后空隙时，车辆和道路段一起进入 `WAIT` 并暂停。
- 并线使用曲线路径和连续 `x/y` 坐标，车辆与道路段共用同一段贝塞尔曲线、目标车道和进度；车身扫掠、相对速度、玩家安全区和障碍都参与判断。
- 进入主路后保留短暂的流速适应段；需要去 L2/L3/L4 时，复用现有相邻车道预约和逐段超车逻辑。
- 合流完成后道路段进入 `EXIT`，与绑定车辆一起移出视野并回收；排队数量只显示在 HUD，不生成预览道路。
- 合流道路段只提供实际可读的路面导流，不改变主路车道的碰撞边界；车辆碰撞和空隙判定仍由交通逻辑负责。
- 主路低密度且玩家前方畅通时，额外的 `REAR CHASE` 激进车从屏幕底部进入；该车辆优先追向玩家当前车道，接近后复用真实空隙和相邻车道超车逻辑。

## 从原型到正式美术

本阶段的 SVG 是自制、可重复、可平铺的灰盒材质源稿；窗口运行时优先读取 SVG，无窗口或导入器不可用时生成同尺寸的 ImageTexture 兜底，因此烟测不依赖 SVG 导入器。它不等于正式像素资产。玩法稳定后，应保持尺寸比例与颜色角色，把 SVG 栅格化为经过 3×3 平铺检查的 PNG / atlas，并在最近邻采样下验收；车辆、道具、特效、字体和 HUD 仍然独立于道路材质，以便各自完成像素动画和状态反馈。
